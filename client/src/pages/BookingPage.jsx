import { useParams } from 'react-router-dom'
import PlacePage from './PlacePage'

const BookingPage = () => {
    const { id } = useParams()

    return (
        <div>
            Hello

        </div>
    )
}

export default BookingPage
